[build]
builder = "nixpacks"
buildCommand = "npm install && npx prisma generate && npm run build --prefix server"

[deploy]
startCommand = "npx prisma migrate deploy && node server/dist/index.js"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"
