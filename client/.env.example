# Only needed for Vercel / production build. Leave empty for local `npm run dev`.
VITE_API_URL=
# Supabase Auth (Authentication → URL, anon public key). Confirm email must be ON.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
